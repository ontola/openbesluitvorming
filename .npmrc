# esbuild ships its native binary as an optional dependency, so its skipped
# postinstall doesn't break the build. Don't let pnpm hard-fail on it.
strict-dep-builds=false
# Skip the pre-`pnpm run` deps verification — it also hard-fails on the
# ignored esbuild build script, which blocks `pnpm dev`.
verify-deps-before-run=false
